---
description: طوّر الفريق نفسه وسجّل التطوير في لوج الديسكتوب
---
Apply the `self-improve` skill.
Review this session: repeated mistakes, missing capabilities, better approaches found, agents/skills created.
Edit the specific owning files (max 3 edits), mirror to `.agents/` and to the global `%USERPROFILE%\.claude\`,
then append the entries to `%USERPROFILE%\Desktop\claude-team-log.md`.
Report one line per change. Nothing else.
