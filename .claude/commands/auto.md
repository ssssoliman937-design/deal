---
description: شغّل الأوتوبايلوت — ينفّذ لوحده ويعمل الوكلاء الناقصة
argument-hint: [المهمة]
---
Task: $ARGUMENTS

Hand to `autopilot`. Full authority except: money, publishing, deleting data, touching a live device or live site — those need one-line confirmation.
If a needed agent or skill doesn't exist, build it (`auto-provision`), register it in `AGENTS.md`, mirror it to `.agents/`, then use it.
End with: what was done (≤3 bullets) + anything new that was added to the team + next step.
