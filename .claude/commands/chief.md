---
description: المنظّم — يقسّم أي مهمة ويوزّعها على الوكلاء ويحدد الموديل لكل خطوة
argument-hint: [المهمة]
---
Task: $ARGUMENTS

Hand to `chief`. Deep think internally, then output the ownership table only:
who owns each part, which model each step runs on, and anything newly built.
Big or unclear? route through `planner` first. Apply `token-budget` throughout.
Then execute and report in ≤3 bullets.
