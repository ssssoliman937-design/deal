---
description: اعمل صفحة هبوط للأداة + خطة ترويج
argument-hint: [اسم الأداة]
---
Tool: $ARGUMENTS

`web-dev` + `designer` + `frontend-design`: build the landing page (hero one-liner, screenshot slot, one download button with version/size/date, requirements, trust section with SHA-256 + VirusTotal + SmartScreen note, changelog, contact).
Then `growth` + `humanizer`: one launch post per platform (XDA / Reddit / YouTube description), written like a human wrote it.
